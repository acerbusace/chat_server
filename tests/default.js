/* jshint esversion:6  */

/* DEFAULT TEST
 * It checks out!
 */

const should = require('should');

describe('addition', function() {
    // 'use strict';
    it('should add 1+1 correctly', function(done) {
        val = 2;
        val.should.equal(2);
        done();
    });
});
